import Unauthorized from "@/components/unauthorised/unauthorised";

interface UnauthorizedProps {}

const UnauthorizedPage = ({}: UnauthorizedProps) => {
  return <Unauthorized />;
};

export default UnauthorizedPage;
